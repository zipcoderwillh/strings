/**
 * Created by willhorton on 2/19/16.
 */
public class MyStringUtils {

    // Combine an array of characters into one String, separated by commas
    public static String combineArrays(char[] input) {

        String result = "";
        return result;

    }

    // Combine an array of Strings into one String, separated by commas
    public static String combineArrays(String[] input) {

        String result = "";
        return result;

    }

    // Break a String up into an array of Strings, one for each line in the original String
    public static String[] splitString(String input) {

        String[] result = new String[3];
        return result;

    }

    // Given a string, rEVERSE-cAPITALIZE eVERY wORD iN tHE sTRING aND rETURN tHE rESULT.
    public static String reverseCapitalize(String input) {
        String result = "";
        return result;
    }

    // Given a String, reverse each word in the string and return the result.
    public static String reverseWords(String input) {
        String result = "";
        return result;
    }

    // Given a String, remove all whitespace and return a string containing each word from the original string on a separate line
    public static String removeWhiteSpace(String input) {
        String result = "";
        return result;
    }

    // Given a string, return an array of every possible substring (every sequence of consecutive characters within the string)
    public static String[] subStrings(String input) {
        String[] result = new String[5];
        return result;
    }

    // Given an array of Strings, return a string containing each array element on a new line, right justified, truncated to no
    // more than 80 characters, followed by a hexadecimal representation of the number of characters in the original string
    // (you should have two columns of text; headings are optional).
    public static String formatStrings(String[] input) {
        String result = "";
        return result;
    }

}
